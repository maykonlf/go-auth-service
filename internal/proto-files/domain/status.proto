syntax = "proto3";

package domain;

option go_package = "github.com/maykonlf/go-auth-service/internal/entities";

enum AccountStatus {
    INACTIVE = 0;
    ACTIVE = 1;
    BLOCKED = 2;
    SUSPENDED = 3;
}